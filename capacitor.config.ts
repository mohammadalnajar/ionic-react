import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'first-ionic-app-mo-97',
    appName: 'ionic-project',
    webDir: 'build',
    bundledWebRuntime: false
};

export default config;
