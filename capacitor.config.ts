import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.firstionicappmo97',
    appName: 'ionic-project',
    webDir: 'build',
    bundledWebRuntime: false
};

export default config;
